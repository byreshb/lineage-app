{
  "name": "lineage-app",
  "version": "1.0.0",
  "description": "SSRS Lineage Tracking System - Full Stack Application",
  "private": true,
  "scripts": {
    "start": "npm run clean && concurrently --kill-others -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\"",
    "clean": "kill-port 3000 8080 || true",
    "backend": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "install-all": "npm install && npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "kill-port": "^2.0.1"
  }
}
